language: java
jdk:
  - oraclejdk7
  - oraclejdk8
dist: precise

script: "mvn --show-version --errors --batch-mode -Prun-its clean verify"

cache:
    directories:
    - $HOME/.m2
branches:
    except:
        - gh-pages
notifications:
    email:
        - rfscholte@apache.org
